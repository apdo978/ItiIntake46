في قواعد البيانات، خصوصًا في **SQL**، الأوامر بتتقسم لمجموعات حسب الغرض منها. أشهر المجموعات:

---

## **1. DDL – Data Definition Language**

دي أوامر خاصة بـ **تعريف وهيكلة قاعدة البيانات** (إنشاء، تعديل، حذف الجداول والهياكل).
أمثلة:

* `CREATE` → لإنشاء قاعدة بيانات أو جدول جديد.
* `ALTER` → لتعديل هيكل جدول (إضافة عمود، تغيير نوع بيانات).
* `DROP` → لحذف جدول أو قاعدة بيانات بالكامل.
* `TRUNCATE` → لمسح كل البيانات من جدول مع الاحتفاظ بالهيكل.

💡 الفكرة: DDL بيغير **هيكل** قاعدة البيانات، مش البيانات نفسها.

---

## **2. DML – Data Manipulation Language**

دي أوامر خاصة بـ **التعامل مع البيانات نفسها** (إضافة، تعديل، حذف).
أمثلة:

* `INSERT` → لإضافة بيانات جديدة.
* `UPDATE` → لتعديل بيانات موجودة.
* `DELETE` → لحذف بيانات.

💡 الفكرة: DML بيتعامل مع **المحتوى** جوه الجداول، مش الهيكل.

---

## **3. DQL – Data Query Language**

تركّز على **قراءة البيانات فقط** من الجداول.

* `SELECT` → لعرض أو استعلام عن البيانات.

💡 الفكرة: DQL بيجيب البيانات بس، من غير ما يغيرها.

---

## **4. DCL – Data Control Language**

للتحكم في **صلاحيات المستخدمين**.

* `GRANT` → إعطاء صلاحيات.
* `REVOKE` → سحب صلاحيات.

---

## **5. TCL – Transaction Control Language**

للتحكم في **المعاملات (Transactions)** داخل قاعدة البيانات.

* `COMMIT` → حفظ التغييرات.
* `ROLLBACK` → التراجع عن التغييرات.
* `SAVEPOINT` → تحديد نقطة حفظ وسط المعاملة.

---

📌 باختصار:

| النوع   | الغرض               |
| ------- | ------------------- |
| **DDL** | هيكل قاعدة البيانات |
| **DML** | تعديل البيانات      |
| **DQL** | استعلام عن البيانات |
| **DCL** | التحكم في الصلاحيات |
| **TCL** | التحكم في المعاملات |

---

لو تحب، أقدر أعملك **جدول تلخيصي** فيه كل الأوامر المشهورة مع مثال لكل واحد عشان تحفظهم بسهولة.
